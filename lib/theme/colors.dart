import 'package:flutter/material.dart';

class AppColors {
  // Brand & Style
  static const Color matteBlack = Color(0xFF121212);
  static const Color deepCharcoal = Color(0xFF1A1A1A);
  static const Color elegantGold = Color(0xFFD4AF37);
  static const Color crispWhite = Color(0xFFFFFFFF);
  
  // Borders
  static const Color borderStroke = Color(0xFF2C2C2C);
  
  // Specific Use Cases
  static const Color textPrimary = crispWhite;
  static const Color textSecondary = Color(0xFFE5E2E1); // Derived from design variables
  static const Color background = matteBlack;
  static const Color surface = deepCharcoal;
  
  static const Color error = Color(0xFFFFB4AB);
}
